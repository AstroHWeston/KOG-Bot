{
  "name": "kog-bot-tomfoolery",
  "version": "0.0.3",
  "description": "Discord utility for the Kleiner Oil Group.",
  "main": "build/src/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "start:prod": "node .",
    "start:dev": "node . --debug",
    "start:ci": "pnpm run build && echo Successfully built! && node . --ci",
    "deploy:prod": "pnpm run build && echo Successfully built! && node build/src/deploy.js",
    "deploy:ci": "pnpm run build && echo Successfully built! && node build/src/deploy.js --ci"
  },
  "keywords": [],
  "author": "Asynchronite & Sheepboi546",
  "license": "AGPL-3.0-only",
  "dependencies": {
    "@discordjs/rest": "^2.4.3",
    "discord.js": "^14.18.0",
    "knex": "^3.1.0"
  },
  "devDependencies": {
    "@sentry/node": "^9.7.0",
    "@sentry/profiling-node": "^9.7.0",
    "mysql2": "^3.14.0",
    "nodemon": "^3.1.9",
    "toml": "^3.0.0"
  }
}
